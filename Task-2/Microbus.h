#pragma once
#include <string>
#include "Vehicle.h"

namespace {
	using std::string;
}

class Microbus : public Vehicle {

public:
	//Microbus(int cap, int gasPerKM, string modName) : Vehicle(cap, gasPerKM), modelName(modName) {};
	Microbus(int cap, int gasPerKM, string modName)
	{

	}

};